# anything-memory-plugin

- Keep this repo standalone. Do not depend on sibling-folder package internals.
- Integration contract with `anything-agent` is plugin module path in `.anything-agent/plugins.json`.
- Prefer CLI/text-first primitives and optional behavior.
- Verify changes with `npm test` before closing work.
